{
  "name": "golden-list",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "apiRest",
    "golden-list-app"
  ],
  "main": "index.js",
  "scripts": {
    "build": "npm run build --workspace=golden-list-app",
    "start:api": "npm run start --workspace=apiRest",
    "dev:api": "npm run build && npm run dev --workspace=apiRest"
  },
  "eslintConfig": {
    "env": {
      "jest": true,
      "cypress/globals": true
    },
    "plugins": [
      "cypress"
    ]
  },
  "devDependencies": {
    "eslint-plugin-cypress": "2.12.1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}